import 'package:flutter/material.dart';

class HeightBetween extends StatelessWidget {
  const HeightBetween({super.key});

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 5.0,
    );
  }
}
